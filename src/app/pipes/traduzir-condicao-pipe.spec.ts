import { TraduzirCondicaoPipe } from './traduzir-condicao-pipe';

describe('TraduzirCondicaoPipe', () => {
  it('create an instance', () => {
    const pipe = new TraduzirCondicaoPipe();
    expect(pipe).toBeTruthy();
  });
});
